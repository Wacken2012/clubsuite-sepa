Inter-App Communication (Sepa)

Dieses Dokument beschreibt, wie Sepa Events sendet und empfängt.

Events: Basic, Callback, RequestData

Beispiel:
```
$eventService->dispatchRequestDataEvent(function($data){ /* handle */ });
```
